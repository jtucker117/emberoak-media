# ---- Cloudinary (your single account) ----
CLOUDINARY_CLOUD_NAME=wrjc0nyq
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret   # keep secret — server-only, never in the website

# ---- Auth ----
SESSION_SECRET=paste_a_long_random_string_here

# ---- Studio owner login(s) for managing the public portfolio ----
# JSON array. Use "password" for quick start, or "passwordHash" (npm run hash "pw") for security.
CLIENTS_JSON=[{"slug":"emberoak","name":"Ember & Oak","folder":"emberoak","password":"change-me"}]

# ---- Client delivery galleries (full-res downloads for the studio's clients) ----
# If ALLOW_OPEN_DELIVERIES=true, any folder deliveries/<code> opens by its code (no pin).
# For pin-protected or titled galleries, list them here:
GALLERIES_JSON=[{"code":"smith-wedding","title":"Smith Wedding","pin":"1234","folder":"deliveries/smith-wedding"}]
ALLOW_OPEN_DELIVERIES=true
DELIVERIES_ROOT=deliveries

# ---- CORS: which site origins may call this API (use * if the site is served by this same app) ----
ALLOWED_ORIGINS=*

# ---- Contact form -> email ----
# Inquiries are ALWAYS written to /data/inquiries.json and shown in Studio Admin >
# Inbox. These vars only add the email notification on top; leave them unset and
# nothing is lost, you just have to check the Inbox tab.
# SMTP_PASS must be a Google APP PASSWORD (myaccount.google.com > Security >
# 2-Step Verification > App passwords), NOT the account password.
# SMTP_USER must be the REAL Google account you sign in with. A send-as ALIAS
# cannot authenticate — Google rejects it with "Username and Password not
# accepted", which looks exactly like a wrong password.
SMTP_USER=jordan@thetucker.family
SMTP_PASS=your_16_char_google_app_password
# The address mail appears to come from. A Gmail send-as alias is fine here.
MAIL_FROM=jordan@emberandoak.media
# Optional: where inquiries land. Defaults to MAIL_FROM.
# INQUIRY_TO=jordan@emberandoak.media
# Optional: defaults are Gmail's.
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=465

# ---- Website assistant (booking intake chat) ----
# Unset ANTHROPIC_API_KEY and the chat bubble simply does not render; the contact
# form is unaffected. The assistant gathers details and submits them through the
# same pipeline as the form. It is instructed never to quote a price.
ANTHROPIC_API_KEY=sk-ant-...
# Optional: defaults to claude-opus-5
# CHAT_MODEL=claude-opus-5
